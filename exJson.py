import json

x = {
  "name": "John",
  "age": 30,
  "married": True,
  "divorced": False,
  "children": ("Ann","Billy"),
  "pets": None,
  "cars": [
    {"model": "BMW 230", "mpg": 27.5},
    {"model": "Ford Edge", "mpg": 24.1}
  ]
}

# transfer an object to json
print(json.dumps(x))

# loads Convert from JSON to Python
# json
x =  '{ "name":"John", "age":30, "city":"New York"}'

# parse x:
y = json.loads(x)

# the result is a Python dictionary:
print(y["age"])

# this define the indentation for redabitly and sort the key values aplabaticly 
json.dumps(x, indent=4, sort_keys=True)
